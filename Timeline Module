<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WIKD Process Section</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;600;700;800&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        /* --- CORE WIKD VARIABLES (From your target site) --- */
        :root {
            --color-background: #0A0A0A;
            --color-text: #EAEAEA;
            --color-text-muted: #888888;
            --color-primary: #FF3B30;
            --color-surface: #141414;
            --color-border: #2A2A2A;
            --font-heading: 'Rajdhani', sans-serif;
            --font-body: 'Inter', sans-serif;
        }

        body {
            background-color: var(--color-background);
            color: var(--color-text);
            font-family: var(--font-body);
            position: relative;
        }

        /* Noise Texture (From your target site) */
        body::before {
            content: "";
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            pointer-events: none; z-index: 9999;
            opacity: 0.08;
            background-size: 100px 100px;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
        }

        /* Typography Utilities */
        .font-heading { font-family: var(--font-heading); }
        .font-body { font-family: var(--font-body); }
        
        .h2 {
            font-family: var(--font-heading) !important;
            font-weight: 700 !important;
            line-height: 1.2 !important;
            text-transform: uppercase !important;
            color: white !important;
            font-size: clamp(2rem, 5vw, 3rem);
        }

        .p-muted {
            font-family: var(--font-body);
            color: var(--color-text-muted) !important;
            line-height: 1.6;
        }

        /* Container Utility */
        .mobile-scaled-container {
            width: 92%;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        /* --- PROCESS SPECIFIC STYLES --- */
        
        /* The container for the number */
        .process-step-box {
            width: 6rem; /* 24 tailwind equivalent */
            height: 6rem;
            background-color: var(--color-surface);
            border: 1px solid var(--color-border);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem auto;
            position: relative;
            z-index: 10;
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            
            /* WIKD GEOMETRY: The signature angled clip used on buttons */
            clip-path: polygon(0 0, 100% 0, 100% 100%, 15px 100%, 0 85%);
        }

        .process-number {
            font-family: var(--font-heading);
            font-weight: 800;
            font-size: 2.25rem;
            color: var(--color-text-muted); /* Start muted */
            transition: color 0.3s ease;
            line-height: 1;
        }

        /* Hover Group Logic */
        .process-item:hover .process-step-box {
            border-color: var(--color-primary);
            box-shadow: 0 0 25px rgba(255, 59, 48, 0.3);
            transform: translateY(-5px);
        }

        .process-item:hover .process-number {
            color: var(--color-primary);
        }

        /* The Connecting Line */
        .process-line {
            position: absolute;
            top: 3rem; /* Center of the 6rem box */
            left: 0;
            width: 100%;
            height: 1px;
            background: var(--color-border);
            z-index: 0;
            /* Hide on mobile via CSS grid logic or media queries */
        }
        
        /* Gradient fade for the line */
        .process-line::after {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: linear-gradient(90deg, 
                var(--color-background) 0%, 
                transparent 15%, 
                transparent 85%, 
                var(--color-background) 100%
            );
        }

        /* Text Styling */
        .process-title {
            color: white;
            font-family: var(--font-heading);
            font-weight: 700;
            text-transform: uppercase;
            font-size: 1.5rem;
            margin-bottom: 0.75rem;
            transition: color 0.3s ease;
        }

        .process-item:hover .process-title {
            color: white; /* Or primary if you want title to light up */
        }
        
        /* Mobile adjustments */
        @media (max-width: 768px) {
            .process-line { display: none; }
        }
    </style>
</head>
<body>

    <!-- WIKD PROCESS SECTION -->
    <section id="process-section" class="py-20" style="background-color: var(--color-background);">
        <div class="mobile-scaled-container">
            
            <!-- Section Header -->
            <div class="text-center mb-16">
                <h2 class="h2">How It Works</h2>
                <p class="p-muted max-w-2xl mx-auto mt-2">
                    A streamlined experience from configuration to delivery. We handle the details so you can enjoy the drive.
                </p>
            </div>

            <!-- Process Grid -->
            <div class="relative grid grid-cols-1 md:grid-cols-3 gap-12 text-center">
                
                <!-- Connecting Line (Desktop) -->
                <div class="hidden md:block process-line"></div>

                <!-- Step 01 -->
                <div class="process-item relative group cursor-default">
                    <div class="process-step-box">
                        <span class="process-number">01</span>
                    </div>
                    <h3 class="process-title">Configure</h3>
                    <p class="p-muted text-sm leading-relaxed max-w-xs mx-auto">
                        Use our instant quote tool to build your package and get accurate pricing in seconds.
                    </p>
                </div>

                <!-- Step 02 -->
                <div class="process-item relative group cursor-default">
                    <div class="process-step-box">
                        <span class="process-number">02</span>
                    </div>
                    <h3 class="process-title">Drop Off</h3>
                    <p class="p-muted text-sm leading-relaxed max-w-xs mx-auto">
                        Bring your vehicle to our McKinney studio. We confirm details and begin the transformation.
                    </p>
                </div>

                <!-- Step 03 -->
                <div class="process-item relative group cursor-default">
                    <div class="process-step-box">
                        <span class="process-number">03</span>
                    </div>
                    <h3 class="process-title">Drive</h3>
                    <p class="p-muted text-sm leading-relaxed max-w-xs mx-auto">
                        Pick up your showroom-ready vehicle, protected and styled to absolute perfection.
                    </p>
                </div>

            </div>
        </div>
    </section>

</body>
</html>
